Firstly initialized the git into local machine:
  git init

Next, Clone or copy the project from the remote repository to your local repo using:
    git clone url


Add the file in the folder and then add into the Git repository:
  git add filename.txt
